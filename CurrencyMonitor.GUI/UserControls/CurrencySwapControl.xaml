<UserControl
    x:Class="CurrencyMonitor.GUI.UserControls.SwitchCurrencyControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CurrencyMonitor.GUI.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel HorizontalAlignment="Center" 
                VerticalAlignment="Center" 
                Margin="0 0 0 -8"
                Spacing="10">

        <Button x:Name="SwitchCurrencyButton"
                Width="48"
                Height="48"
                HorizontalAlignment="Center"
                Foreground="Transparent"
                Click="SwitchCurrencyButton_OnClick">
            <Image x:Name="ImageExchange"
                   Stretch="UniformToFill"
                   HorizontalAlignment="Center"
                   Source="{x:Bind ImageSource}"/>
        </Button>

        <TextBlock Text="Развернуть"/>

    </StackPanel>

</UserControl>
